| 决策主题 | 已确认决策 | 来源 | 确认日期 |
| --- | --- | --- | --- |
| 测试约定 | 对账测试独立为 tests/strategy/hedge-reconciliation.test.ts，使用 fake 网关与临时或内存 SQLite。先红后绿。覆盖查所失败/部分成功、planned 找不到、无市价腿、市价仍 open、等量 HEDGED、1 对 0.6 的 need_gtc、已有 open GTC、GTC 拒撤、双零 NO_FILL、缺均价。旧协调器/监控器测试改为断言未经 run 不能终态或补 GTC。 | 用户明确回答 | 2026-09-05 |
| 并发单边拒单 | CONCURRENT 模式下，一条市价腿有正成交、另一条市价腿明确 rejected 且零成交时，完成三方对账后禁止自动补 GTC，写入 HEDGE_INCOMPLETE，失败码为 INCONSISTENT_ORDER_STATE。 | 用户明确回答 | 2026-09-05 |
| 缺陷修订方案 | 采用完整修订方案：HedgeReconciliation.run 同时处理 EXECUTING 与 WAITING_HEDGE，pending 保持进入状态；仅空订单集合可返回 awaiting_market_submission，非空非法角色拓扑不得授权下单；按查回、三方一致、角色拓扑、市价状态、GTC 状态、残差结果的互斥顺序判定；GTC 委托量对比补单前市场残差，剩余量对比计入 GTC 成交后的当前残差，完全成交的 canceled GTC 可 HEDGED，unknown 必须 pending；need_gtc 前以独立精确小数上下文验证残差的数量、步长、合约张数和名义金额，无法精确交易则 HEDGE_INCOMPLETE/HEDGE_RESIDUAL_NOT_TRADABLE；持久化本地订单提交结论，确定未提交且按 client ID 确认所上不存在后才可终态，普通 planned 未找到仍 pending；快照仅语义变化时落库并由实际落库者唯一记录状态与终态事件；pending 返回类型化原因、相关订单或交易所及 exposureKnown；written 仅在状态 CAS 成功后返回；补齐恢复、脏拓扑、部分成交、不可交易残差、确定未提交、事件去重、CAS 竞争及互斥决策表测试。对账模块仍不下单，不新增策略状态或订单角色。 | 用户明确回答 | 2026-09-05 |
